# Constants

Shared constant values used across schemas and application layers.

## Enums

`enums.ts` exports typed constant objects maps used by Zod nativeEnum and application code.

Example:

```
import { USER_ROLE_MAP } from '@int/schema';
```
